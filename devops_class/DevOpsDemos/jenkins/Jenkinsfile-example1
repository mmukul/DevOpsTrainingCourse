pipeline {
  agent any

   stages {
     stage('build') {
        steps {
            echo 'build pipeline...'
        }
     }
     stage('test') {
        steps {
            echo 'test pipeline...'
        }
     }
     stage('deploy') {
        steps {
            echo 'deploy pipeline...'
        }
     }
   }
}
